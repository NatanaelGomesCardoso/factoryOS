# Technical Spec - Capsule Cost Root Cause V0

Implementar `app/capsule_cost_diagnosis.py` e comando `capsule-cost-diagnose`.

Entradas:

- `--e2e-report`.

Saída:

- JSON compacto no terminal;
- report completo em `reports/capsule-cost-diagnosis/`.
