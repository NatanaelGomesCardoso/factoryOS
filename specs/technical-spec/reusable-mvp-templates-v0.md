# Technical Spec - Reusable MVP Templates V0

## Implementação

- criar registry local em Python;
- expor `list`, `show` e `validate` pela CLI;
- reutilizar o registry no intake;
- manter templates versionados no repo.

