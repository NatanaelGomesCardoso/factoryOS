# Reuse First - Cheap Capsule Prompt Slimming V0

Reaproveitar `create_capsule`, `run_codex_capsule_run` e `cheap-task-factory-e2e`.

Decisão: adicionar `capsule_mode=ultra_slim` e manter o modo padrão compatível.
