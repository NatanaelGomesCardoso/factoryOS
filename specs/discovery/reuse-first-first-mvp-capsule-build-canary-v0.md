# Reuse First Discovery - First MVP Capsule Build Canary V0

## Ideia

Validar um build mínimo em cápsula antes de qualquer aplicação no repo real.

## O que reutilizar

- o build plan gerado na sprint 061;
- `create_capsule`;
- `run_codex_capsule_run`, `run_codex_capsule_export_plan`, `run_codex_capsule_diff` e `run_codex_capsule_status`;
- um artefato canário mínimo e seguro.

## Decisão esperada

- execução econômica;
- nada aplicado fora da cápsula;
- export plan pronto para o gate de aplicação.

