# Reuse First Discovery - First MVP Dry-Run Build Plan V0

## Ideia

Transformar o intake inicial de MVP em um plano de build controlado, ainda sem execução real.

## O que reutilizar

- report de `project-intake`;
- padrões de routing `docs_only` e `code_small` para capsule;
- separação explícita entre backend e frontend;
- contrato de report local em JSON.

## Decisão esperada

- plano de build dry-run;
- nenhuma aplicação real;
- base para canary de capsule e gate humano.

